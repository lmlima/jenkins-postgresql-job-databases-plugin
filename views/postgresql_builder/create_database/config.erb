<%
  f = taglib("/lib/form")

  desc = 'Environment variables available in shell build steps: $PGSQL_DATABASE, $PGSQL_USER, $PGSQL_PASSWORD, $PGSQL_HOST, $PGSQL_PORT.'

  f.entry :title => 'Database name', :field => 'database', :description => desc do
    f.textbox
  end

  f.advanced do
    f.entry :title => 'Database user name', :field => 'job_postgresql_user' do
      f.textbox
    end

    f.entry :title => 'Database user password', :field => 'job_postgresql_password' do
      f.textbox
    end
  end
%>
